#include <stdio.h>

int main(){
    int day=30;
    float daily_uses[30];
    float total_uses =0.0;
    for (int  i = 0; i < day; i++)
    {
        printf("enter water uses for day %f\n",i+1);
        scanf("%f",&i);
        
    }
    
    return 0;
}